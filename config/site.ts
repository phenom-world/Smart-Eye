import { Metadata } from 'next';

export const siteConfig: Metadata = {
  title: {
    default: 'SparkPlug Evv',
    template: 'SparkPlug Evv | %s',
  },
  description: 'SparkPlug Evv is an evv app for all',
};
